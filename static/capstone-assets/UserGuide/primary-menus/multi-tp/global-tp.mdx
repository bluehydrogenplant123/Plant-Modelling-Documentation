---
title: Global TP
sidebar_position: 2
description: Use the Global TP secondary button from the Multi-TP menu.
---

# Global TP

Use **Global TP** when you need to define the same time-period range structure for all nodes in the diagram.

## Where To Find It

1. Open an existing diagram.
2. Select the **Multi-TP** primary menu.
3. Click **Global TP** in the secondary button row.

## What It Opens

The **Global TP** modal opens. It includes the current TP count, fields for adding or deleting TP counts, a **Build Grid** button, range rows, **duration**, **duration unit**, and **Apply to All Nodes**.

![Global TP modal with current TP count, add and delete count fields, Build Grid, and Close controls](../../images/multi-tp-global-tp.png)

## Basic Steps

1. Open **Global TP**.
2. Enter how many TPs to add or delete.
3. Click **Build Grid**.
4. Review each range row, including **from tp**, **to tp**, **duration**, and **duration unit**.
5. Adjust the range values where needed.
6. Click **Apply to All Nodes**.
7. Click **Close** when the modal finishes applying the changes.

## Result

The global TP range grid is applied to the nodes in the current diagram. When
a Base diagram is expanded to Multi-TP, eligible human-entered Base values are
copied into the new range for every node. The diagram then uses the updated TP
count, ranges, durations, and duration units for later Multi-TP work.

After **Apply to All Nodes**, inspect representative values, save the diagram,
and reopen it. For a duplicated diagram, also compare the original and the copy:
the copy must retain its own values and TP rows without changing the source.

## Related Pages

- [Multi-TP Menu overview](../multi-tp)
- [TP Node - Model Version Control](./tp-node-model-version-control)
- [TP Specs](./tp-specs)
- [TP Analysis Menu](../tp-analysis)
