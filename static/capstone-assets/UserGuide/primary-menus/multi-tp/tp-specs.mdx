---
title: TP Specs
sidebar_position: 3
description: Use the TP Specs secondary button from the Multi-TP menu.
---

# TP Specs

Use **TP Specs** when you need to review or edit specification values for time-period-specific node variables.

## Where To Find It

1. Open an existing diagram with Multi-TP ranges.
2. Select the **Multi-TP** primary menu.
3. Click **TP Specs** in the secondary button row.

## What It Opens

The **TP Specs** modal opens in Multi-TP mode. It shows TP-specific specification data in a grid.

The modal is read-only when the **MTP.TPSpecs** computing rule is read-only. In that state, use the grid for review and do not expect editable fields or saving.

## Basic Steps

1. Open **TP Specs**.
2. Review rows by network, node, port, port variable, TP range, value, spec, bounds, and unit.
3. If the grid is editable, update the enabled values.
4. Click **Save** when edits are enabled and complete.
5. Click **Close** when finished.

## Result

Editable TP specification changes are saved for the current diagram. If the modal is read-only, no TP Spec data is changed.

## Related Pages

- [Multi-TP Menu overview](../multi-tp)
- [Global TP](./global-tp)
- [TP Node - Model Version Control](./tp-node-model-version-control)
- [Specs](../model/specs)
